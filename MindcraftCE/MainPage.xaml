<Page x:Class="MindcraftCE.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:MindcraftCE"
      Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
  <Grid Background="Transparent">
        <NavigationView x:Name="nvSample"
                        PaneDisplayMode="Left"
                        IsBackButtonVisible="Collapsed"
                        IsBackEnabled="False"
                        ItemInvoked="nvSample_ItemInvoked"
                        IsSettingsVisible="False"
                        PaneClosing="nvSample_SizeChanged"
                        PaneOpening="nvSample_SizeChanged"
                        x:FieldModifier="public">

            <!--<NavigationView.Header>
                <TextBlock Text="mindcraft-ce" FontSize="24" FontWeight="Bold" Margin="12,0,0,0"/>
            </NavigationView.Header>-->

            <NavigationView.MenuItems>
                <NavigationViewItem Content="Play"     Icon="Play"        Tag="MindcraftCE.Views.PlayView"/>
                <NavigationViewItemSeparator/>
                <NavigationViewItem Content="Agents"   Icon="People"      Tag="MindcraftCE.Views.AgentView"    IsEnabled="False"/>
                <NavigationViewItem Content="Settings" Icon="Setting"     Tag="MindcraftCE.Views.SettingsView" IsEnabled="False"/>
                <NavigationViewItem Content="API Keys" Icon="Permissions" Tag="MindcraftCE.Views.KeysView"     IsEnabled="False"/>
                <NavigationViewItemSeparator/>
                <NavigationViewItem Content="Update"   Icon="Download"    Tag="MindcraftCE.Views.UpdatesView"/>
                <NavigationViewItemSeparator/>
                <NavigationViewItem Content="Ollama"                      Tag="MindcraftCE.Views.OllamaView">
                    <NavigationViewItem.Icon>
                        <BitmapIcon UriSource="https://ollama.com/public/ollama.png" ShowAsMonochrome="True"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.MenuItems>

            <NavigationView.PaneFooter>
                <Grid x:Name="agentDisplay" Padding="8" Margin="0,8" Visibility="Visible" Tapped="agentDisplay_Tapped" Background="Transparent">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <!-- <ColumnDefinition Width="Auto"/> -->
                    </Grid.ColumnDefinitions>

                    <Image x:Name="agentDisplayImage" Width="40" Height="40" HorizontalAlignment="Center" Margin="5"/>

                    <StackPanel Grid.Column="1" Margin="12,0,0,0" VerticalAlignment="Center" x:Name="agentDisplayTextPanel">
                        <TextBlock x:Name="agentDisplayCount" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" Text="2 agents online"/>
                        <TextBlock x:Name="agentDisplayStatus" FontSize="12" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" TextTrimming="CharacterEllipsis" Text="Connected to 127.0.0.1:55916"/>
                    </StackPanel>

                </Grid>
            </NavigationView.PaneFooter>

            <Frame x:Name="contentFrame" x:FieldModifier="public">
                <Frame.ContentTransitions>
                    <TransitionCollection>
                        <NavigationThemeTransition/>
                    </TransitionCollection>
                </Frame.ContentTransitions>
            </Frame>
        </NavigationView>
    </Grid>
</Page>
