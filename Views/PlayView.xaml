<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="mindcraft_ce.Views.PlayView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:mindcraft_ce.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:mindcraft_ce.Models"
    mc:Ignorable="d">

    <Grid>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>


            <StackPanel Margin="20,20,0,0" Grid.Column="0">
                <TextBlock x:Name="AgentsTextHeader" Text="Agents" FontSize="24" FontWeight="Bold" Margin="30,0,0,10"/>
                <ListView
                    x:Name="agentListView" 
                    ItemsSource="{Binding Agents}"
                    SelectionMode="None"
                    IsItemClickEnabled="True"
                    ItemClick="agentListView_ItemClick"
                    >

                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="models:Agent">
                            <Grid Margin="5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="50"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <CheckBox Grid.Column="0" Padding="0" Margin="10,0" IsChecked="{x:Bind IsChecked, Mode=TwoWay}"></CheckBox>

                                <!-- Name - Model -->
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Grid.Column="1">
                                    <TextBlock Text="{x:Bind Name}" FontWeight="Bold" Margin="0"/>
                                    <TextBlock Text=" - " />
                                    <TextBlock Text="{x:Bind Model}" Foreground="Gray" Margin="5,0"/>
                                </StackPanel>

                                <!-- Edit Button -->
                                <Button Grid.Column="2" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="5,0" Click="EditButton_Click">
                                    <FontIcon Glyph="&#xE70F;" />
                                </Button>

                                <!-- Delete Button -->
                                <Button Grid.Column="3" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="5,0" Click="DeleteButton_Click">
                                    <FontIcon Glyph="&#xE74D;" />
                                </Button>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>

            <Button
                x:Name="playButton"
                Content="Play"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Width="200"
                Height="50"
                Click="playButton_Click"
                Grid.Column="1"
             />

        </Grid>
    </Grid>
</Page>
