<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="mindcraft_ce.Views.PlayView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:mindcraft_ce.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:mindcraft_ce.Models"
    mc:Ignorable="d">

    <Grid>
        <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
             <TextBlock
                VerticalAlignment="Center"
                HorizontalAlignment="Center"
                FontSize="18"
                x:Name="messageBox"
                Visibility="Collapsed"
                Grid.Row="0"
                Margin="20"
            />

            <Button
                x:Name="installButton"
                Content="Install"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Width="200"
                Height="50"
                Click="installButton_Click"
                Grid.Row="1"
                Visibility="Collapsed"
                Margin="20"
             />
        </Grid>


        <Grid x:Name="AgentViewGrid">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>


            <Grid Margin="20,20,0,0" Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <TextBlock x:Name="AgentsTextHeader" Text="Agents" FontSize="24" FontWeight="Bold" Margin="30,0,0,10" Grid.Row="0"/>
                <ScrollViewer Grid.Row="1"
                    VerticalScrollBarVisibility="Auto"
                    HorizontalScrollBarVisibility="Hidden"
                    >
                    <ListView
                        x:Name="agentListView" 
                        ItemsSource="{Binding Agents}"
                        SelectionMode="None"
                        IsItemClickEnabled="True"
                        ItemClick="agentListView_ItemClick"
                        >

                        <ListView.ItemTemplate>
                            <DataTemplate x:DataType="models:Agent">
                                <Grid Margin="5">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="50"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <CheckBox Grid.Column="0" Padding="0" Margin="10,0" IsChecked="{x:Bind IsChecked, Mode=OneWay}" Click="CheckBox_Click"></CheckBox>

                                    <!-- Name - Model -->
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Grid.Column="1">
                                        <TextBlock Text="{x:Bind Name}" FontWeight="Bold" Margin="0"/>
                                        <TextBlock Text=" - " />
                                        <TextBlock Text="{x:Bind ModelInfo}" Foreground="Gray" Margin="5,0"/>
                                    </StackPanel>

                                    <!-- Edit Button -->
                                    <Button Grid.Column="2" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="5,0" Click="EditButton_Click">
                                        <FontIcon Glyph="&#xE70F;" />
                                    </Button>

                                    <!-- Delete Button -->
                                    <Button Grid.Column="3" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="5,0" Click="DeleteButton_Click">
                                        <FontIcon Glyph="&#xE74D;" />
                                    </Button>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </ScrollViewer>
            </Grid>

            <Grid
                Grid.Column="1"
                >
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                    <Button
                    x:Name="playButton"
                    Content="Play"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Top"
                    Width="200"
                    Height="50"
                    Click="playButton_Click"
                    Margin="0,30,0,20"
                    Grid.Row="1"
                 />

                <Button
                    x:Name="copyButton"
                    VerticalAlignment="Top"
                    HorizontalAlignment="Right"
                    Margin="0,0,15,20"
                    Grid.Row="2"
                    Click="copyButton_Click"
                    Visibility="Collapsed"
                >
                    <StackPanel Orientation="Horizontal">
                        <FontIcon Glyph="&#xE8C8;"/>
                        <TextBlock Text="Copy Logs" Margin="5,0,0,0"/>
                    </StackPanel>
                </Button>

                <ScrollViewer
                    x:Name="logScrollViewer"
                    VerticalScrollBarVisibility="Auto"
                    Margin="10,40,10,10"
                    Padding="5"
                    Grid.Row="3"
                >
                    <TextBlock
                        x:Name="logTextBlock"
                        FontSize="16"
                        Margin="20"
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Stretch"
                        Visibility="Visible"
                    />
                </ScrollViewer>
            </Grid>
        </Grid>
    </Grid>
</Page>
